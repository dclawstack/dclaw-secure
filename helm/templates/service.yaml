apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-secure.fullname" . }}-backend
  labels:
    app: {{ include "dclaw-secure.name" . }}
    component: backend
spec:
  type: {{ .Values.service.backend.type }}
  ports:
    - port: {{ .Values.service.backend.port }}
      targetPort: 8031
      protocol: TCP
      name: http
  selector:
    app: {{ include "dclaw-secure.name" . }}
    component: backend

---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-secure.fullname" . }}-frontend
  labels:
    app: {{ include "dclaw-secure.name" . }}
    component: frontend
spec:
  type: {{ .Values.service.frontend.type }}
  ports:
    - port: {{ .Values.service.frontend.port }}
      targetPort: 3031
      protocol: TCP
      name: http
  selector:
    app: {{ include "dclaw-secure.name" . }}
    component: frontend
