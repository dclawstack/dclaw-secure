replicaCount: 1

image:
  repository: ghcr.io/dclawstack/dclaw-secure
  pullPolicy: IfNotPresent
  tag: ""

service:
  type: ClusterIP
  port: 8119

ingress:
  enabled: true
  className: nginx
  annotations: {}
  hosts:
    - host: secure.dclawstack.io
      paths:
        - path: /
          pathType: Prefix
  tls: []

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 100m
    memory: 128Mi

postgresql:
  enabled: true
  primary:
    persistence:
      enabled: true
      size: 10Gi

serviceAccount:
  create: true
  annotations: {}
  name: ""
