apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-secure.fullname" . }}
  labels:
    {{- include "dclaw-secure.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dclaw-secure.selectorLabels" . | nindent 4 }}
