apiVersion: apps/v1
kind: Deployment
metadata:
  name: dclaw-secure-frontend
  namespace: dclaw-core
  labels:
    app.kubernetes.io/name: dclaw-secure
    app.kubernetes.io/part-of: dclaw-platform
    app.kubernetes.io/component: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: dclaw-secure
  template:
    metadata:
      labels:
        app.kubernetes.io/name: dclaw-secure
        app.kubernetes.io/part-of: dclaw-platform
        app.kubernetes.io/component: frontend
    spec:
      containers:
        - name: frontend
          image: docker.io/library/dclaw-secure-frontend:local
          imagePullPolicy: Never
          ports:
            - containerPort: 3031
              name: http
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: "50m"
              memory: "128Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: dclaw-secure
  namespace: dclaw-core
  labels:
    app.kubernetes.io/name: dclaw-secure
    app.kubernetes.io/part-of: dclaw-platform
    app.kubernetes.io/component: frontend
spec:
  type: NodePort
  ports:
    - port: 3031
      targetPort: http
      nodePort: 30031
      name: http
  selector:
    app.kubernetes.io/name: dclaw-secure
