apiVersion: v1
kind: Secret
metadata:
  name: dclaw-secure-secret
  namespace: dclaw-core
  labels:
    app.kubernetes.io/name: dclaw-secure
    app.kubernetes.io/part-of: dclaw-platform
type: Opaque
stringData:
  DATABASE_URL: "postgresql+asyncpg://postgres:postgres@dclaw-shared-postgres:5432/dclaw_secure"
  APP_ENV: "production"
  SECRET_KEY: "dclaw-secure-local-secret-key"
